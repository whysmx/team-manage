services:
  app:
    build: .
    container_name: team-manage-app
    ports:
      - "${APP_PORT:-8009}:${APP_PORT:-8009}"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    restart: always
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////app/data/team_manage.db
